export interface TestimonialProps {
  imageUrl: string;
  name: string;
  status: string;
  headline: string;
  content: string;
  textColor: string;
  backgroundColor: string;
  gridArea: string;
  showQuote?: boolean;
  reducedPadding?: boolean;
  imageBorder?: string;
  isFirstCard?: boolean;
  isFirstRow?: boolean;
}
